// Placeholder smoke test.
// The default Flutter template's counter test doesn't apply to this app —
// replace with meaningful tests as features land.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
