# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
*.iml

# Environment — never commit real keys
.env
.env.local
*.env.local

# Remotion video project
video/node_modules/
video/out/
video/.env*

# macOS
.DS_Store

# IDE
.idea/
*.xml
!.idea/runConfigurations/

# Pub
# pubspec.lock is committed for reproducible CI builds

# Local Netlify folder
.netlify

# Claude Code / agent tooling (per-user local config, not shared)
.claude/

# Wrangler (Cloudflare) local state
.wrangler/

# Supabase CLI local/temp state (auto-generated)
supabase/.temp/
